{
    "name": "basf-coding-challenge",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "npm test --prefix=frontend-app",
        "install-project": "sh shell/install-deps.sh",
        "dev": "concurrently \"npm run start-watch --prefix=api\" \"npm start --prefix=frontend-app\"",
        "build-prod": "npm run build --prefix=frontend-app && rimraf ./api/public && sh shell/place-production-files.sh",
        "start-prod": "npm run start --prefix=api"
    },
    "devDependencies": {
        "concurrently": "^5.3.0",
        "rimraf": "^3.0.2"
    }
}
