{
    "name": "basf-coding-challenge",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "npm test --prefix=frontend-app",
        "install-project": "sh shell/install-deps.sh",
        "dev": "concurrently \"npm run start-watch --prefix=api\" \"npm start --prefix=frontend-app\""
    },
    "devDependencies": {
        "concurrently": "^5.3.0"
    }
}
